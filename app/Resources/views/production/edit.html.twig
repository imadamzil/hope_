{% extends 'base.html.twig' %}

{% block title %} Hope3k | Production {% endblock %}{% block menu %}  <a
        href="{{ path('production_index') }}">
    Production</a>   {% endblock %}   {% block menu_active %}    Modifier {% endblock %}{% block content1 %}
    <h1>Production edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('production_index') }}">Back to the list</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
